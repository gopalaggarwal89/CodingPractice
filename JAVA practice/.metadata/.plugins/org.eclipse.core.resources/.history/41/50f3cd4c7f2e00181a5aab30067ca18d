package info.learn.oops;

import java.util.Date;

public class Test {

	public static void main(String[] args) throws InterruptedException {

//		C a = new C();
//	
//		System.out.println(a.s);
		
		// c.display(); // Cannot call this as parent reference will be used and method
		// not available

	}

	private static void checkImmutable() throws InterruptedException {
		// Immutable example
		ImmutableClass temp = ImmutableClass.getImmutableClass("Gopal", new Date(System.currentTimeMillis()));
		System.out.println(temp.getName() + " : " + temp.getDate()); // Gopal
		
		temp.name.substring(3);
		
		Thread.sleep(1000);
		temp.getDate().setTime(System.currentTimeMillis());
		System.out.println(temp.name + " : " + temp.getDate()); // Gopal
	}

}
