package info.learn.general;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.ArrayList;
import java.util.List;

public class PatternWithStartHash {

	public static void main(String[] args) throws InterruptedException {
		List<String> inputByLine = new ArrayList<String>();
		try {
			// Get the object of DataInputStream
			InputStreamReader isr = new InputStreamReader(System.in, "UTF-8");
			BufferedReader br = new BufferedReader(isr);
			int input = Integer.valueOf(br.readLine());
			int count = input / 2;
			if (input % 2 != 0) {
				count++;
			}
			System.out.println(count); 	

			isr.close();
		} catch (IOException ioe) {
			ioe.printStackTrace();
		}
	}
	
	private String getStarPattern(int count, int length) {
		StringBuilder builder = new StringBuilder();
		for(int i=0;i<count;i++) {
			
		}
		
		return builder.toString();
	}
	
	private String getHashPattern(int count, int length) {
		StringBuilder builder = new StringBuilder();
		for(int i=0;i<count;i++) {
			
		}
		
		return builder.toString();		
	}
}
